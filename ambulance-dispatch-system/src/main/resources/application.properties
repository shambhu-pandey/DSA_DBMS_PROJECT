spring.application.name=ambulance-dispatch
server.port=8086
server.servlet.session.tracking-modes=cookie

# Enable MongoDB for data persistence
# Default (local MongoDB):
# spring.data.mongodb.uri=mongodb://localhost:27017/ambulance_db

## Using MongoDB Atlas with your credentials (directly configured)
# Replace with your confirmed password if different. The '@' character must be encoded as '%40'.
spring.data.mongodb.uri=mongodb+srv://shambhupandey1008_db_user:Shambhu%401008@ambulancedispatch.bncvooh.mongodb.net/ambulance_db?retryWrites=true&w=majority
